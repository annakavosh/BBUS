# Stress tests

Focused sensitivity tests for the separate `belief_update::stress` module live
here. The module is not linked into the real-time update target. Future
long-running, adversarial, and latency harnesses also belong here and must use
an explicit CMake opt-in.
