# Replay tests

Deterministic in-memory replay is covered by `tests/cpp/replay_test.cpp` and
`tests/python/test_replay.py`. Serialized audit-log fixtures belong here once a
serialization format exists; serialization remains outside the update path.
